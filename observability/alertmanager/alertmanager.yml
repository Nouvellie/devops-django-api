global:
  resolve_timeout: 1m

route:
  group_by: ['alertname', 'severity']
  group_wait: 5s
  group_interval: 10s
  repeat_interval: 1h
  receiver: 'null-receiver'

receivers:
  - name: 'null-receiver'